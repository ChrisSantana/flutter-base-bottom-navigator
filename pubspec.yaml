name: flutter_base_bottom_navigator
description: Projeto Flutter base com bottom-navigator

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^0.1.3
  dio: ^3.0.10
  intl: ^0.16.1
  font_awesome_flutter: ^8.8.1
  package_info: ^0.4.1
  provider: ^4.3.2
  rxdart: ^0.24.1
  shared_preferences: ^0.5.8
  sqflite: ^1.3.1
  transparent_image: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/background/